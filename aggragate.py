import numpy as np

a = np.array([
    [10, 20, 30],
    [40, 50, 60],
    [70, 80, 90]
])

a.sum()
a.mean()
a.std()          # standard deviation
a.min(), a.max()
a.argmax()         # big value-এর index
